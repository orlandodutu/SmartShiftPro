from flask import Flask, jsonify, request, send_file from flask_cors import CORS fromflask_sqlalchemy import SQLAlchemy from reportlab.pdfgen import canvas fromreportlab.lib.pagesizes import A4 import io app = Flask(__name__) CORS(app) # Configurazione Database SQLite app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite:///gestione_turni.db'app.config['SQLALCHEMY_TRACK_MODIFICATIONS'] = False db = SQLAlchemy(app) # --- MODELLO DATABASE --- class Dipendente(db.Model): id = db.Column(db.Integer, primary_key=True) nome = db.Column(db.String(50), unique=True, nullable=False) ruolo = db.Column(db.String(20), nullable=False) password = db.Column(db.String(100), default="password123") ore_totali = db.Column(db.Integer, default=0) notti_fatte = db.Column(db.Integer, default=0) ferie = db.Column(db.Integer, default=0) malattia = db.Column(db.Integer, default=0) # Inizializzazione Staff Completo (Inclusa Elena) def inizializza_staff(): staff_nomi = [ ("Orlando", "DEV"), ("Fabiana", "PULIZIE"), ("Marina", "PULIZIE"), ("Angela", "PULIZIE"), ("Carmen", "OSS"), ("Roberto", "OSS"), ("Barbara", "OSS"), ("Vittoria", "OSS"), ("Stefania 2", "OSS"), ("Anna", "INFERMIERA"), ("Stefania", "OSS"), ("Ioana", "OSS"), ("Elena", "OSS")  # <--- Aggiunta Elena qui! ] if Dipendente.query.first() is None: for nome, ruolo instaff_nomi: nuovo = Dipendente(nome=nome, ruolo=ruolo) db.session.add(nuovo) db.session.commit() print("Database creato e staff (con Elena) caricato!") # --- ROTTE API --- @app.route('/api/login', methods=['POST']) def login(): data = request.json user = Dipendente.query.filter_by(nome=data.get('username'), password=data.get('password')).first()if user: return jsonify({ "nome": user.nome, "ruolo": user.ruolo, "stats": {"ore": user.ore_totali, "notti": user.notti_fatte, "ferie": user.ferie, "malattia": user.malattia} }) return jsonify({"errore": "Credenziali errate"}), 401@app.route('/api/genera_report_mensile', methods=['GET']) def genera_pdf(): mese = request.args.get('mese', 'Mese Corrente').upper() buffer = io.BytesIO() c = canvas.Canvas(buffer, pagesize=A4) c.setFont("Helvetica-Bold", 16) c.drawString(50, 800, f"REPORT MENSILE TURNI - {mese}") y = 750 c.setFont("Helvetica-Bold", 10) header = "DIPENDENTE".ljust(20) + "RUOLO".ljust(15) + "ORE".center(10) + "NOTTI".center(10) c.drawString(50, y, header) c.line(50, y-5, 550, y-5) y -= 30 c.setFont("Helvetica", 10) ford in Dipendente.query.all(): linea = f"{d.nome.ljust(20)}{d.ruolo.ljust(15)}{str(d.ore_totali).center(10)}{str(d.notti_fatte).center(10)}" c.drawString(50, y, linea) y -= 20 c.save() buffer.seek(0) return send_file(buffer, as_attachment=True, download_name=f"Report_{mese}.pdf"<span class="undefined" data-copy-service-computed-style="font-family: monospace; font-size: 14px; font-weight: 400; margin: 0px; text-decoration: none; borde